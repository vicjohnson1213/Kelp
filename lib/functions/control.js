var _ = require('lodash');

var control = {
    'if': 'Handled inside the interpreter',
    'begin': 'Handled inside the interpreter',
    'lambda': 'Handled inside the interpreter'
};

module.exports = control;